{
  "name": "ludwig-paie",
  "main": "server.js",
  "dependencies": {
    "cors": "^2.5.3",
    "express": "^4.12.2",
    "ludwig-api": "sgmap/ludwig-api",
    "ludwig-ui": "sgmap/ludwig-ui#v0.1.1"
  },
  "config": {
    "mongoHost": "localhost:27017"
  },
  "scripts": {
    "preinstall": "command -V compass",
    "postinstall": "cd node_modules/ludwig-ui && npm install && grunt build",
    "start": "node server.js",
    "test": "watai tests"
  },
  "devDependencies": {
    "watai": "^0.6.2"
  }
}
